@model IEnumerable<RejestrOsobZaginionych.Models.PersonHistory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Person.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsFound)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LostDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FoundDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Person.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsFound)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LostDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FoundDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PersonHistoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PersonHistoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PersonHistoryId })
        </td>
    </tr>
}

</table>
